@PACKAGE_INIT@
include(CMakeFindDependencyMacro)
find_dependency(HDF5 REQUIRED)
find_dependency(MiniZip REQUIRED)

if(UNIX)
	find_dependency(UUID REQUIRED)
endif()

if(${WITH_JAVA})
	find_dependency(JNI REQUIRED)
	find_dependency(SWIG REQUIRED)
	find_dependency(Java REQUIRED)
endif()

if(NOT TARGET Fesapi::FesapiCpp)
	include("${CMAKE_CURRENT_LIST_DIR}/FesapiCppTargets.cmake")
endif()
